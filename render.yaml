services:
  - type: web
    name: nanobanana-plan-elevation
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: NANOBANANA_API_KEY
        sync: false
